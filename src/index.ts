/**
 * Root package entry.
 *
 * @remarks
 * The root export intentionally exposes shared contracts only.
 * Runtime APIs are available from `@open-core/npc/server` and `@open-core/npc/client`.
 */
export * from './shared'
